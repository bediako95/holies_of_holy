import Layout from "../components/Layout";
import { Flex, Text, Box } from "@theme-ui/components";
const blog = () => {
  return (
    <Layout>
      <Flex
        sx={{
          height: "100vh",
        }}
      ></Flex>
    </Layout>
  );
};

export default blog;
